import 'package:flutter/material.dart';

class SettingsBenutzerdaten extends StatelessWidget {
  const SettingsBenutzerdaten({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Benutzerdaten')),
      body: const Center(child: Text('Platzhalter Benutzerdaten')),
    );
  }
}