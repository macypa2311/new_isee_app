import 'package:flutter/material.dart';

class SettingsBenachrichtigung extends StatelessWidget {
  const SettingsBenachrichtigung({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Benachrichtigung')),
      body: const Center(child: Text('Platzhalter Benachrichtigung')),
    );
  }
}